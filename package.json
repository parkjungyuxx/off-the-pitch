{
  "name": "off-the-pitch-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter off-the-pitch dev",
    "build": "pnpm --filter off-the-pitch build",
    "build:infinite-scroll": "pnpm --filter @bongsik/infinite-scroll build",
    "lint": "pnpm --filter off-the-pitch lint",
    "start": "pnpm --filter off-the-pitch start"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
